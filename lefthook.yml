# Lefthook configuration for Fuji
# Enterprise-grade security and quality checks
# Note: Fuji is Linux-only. Runs in Dev Container on non-Linux platforms

pre-commit:
  commands:
    # Auto-format and stage changes
    format:
      run: |
        if [[ "$OSTYPE" == "linux-gnu"* ]]; then
          # Running on Linux - execute directly
          make fmt
          git add -u '*.rs'
        else
          # Running on macOS - execute in dev container
          devcontainer exec --workspace-folder . make fmt
          git add -u '*.rs'
        fi
      glob: "*.rs"
      fail_text: "Code formatting failed."

    # Quick compilation check
    check:
      run: |
        if [[ "$OSTYPE" == "linux-gnu"* ]]; then
          # Running on Linux - execute directly
          make build-dev
        else
          # Running on macOS - execute in dev container
          devcontainer exec --workspace-folder . make build-dev
        fi
      glob: "*.rs"
      fail_text: "Code has compilation errors."

pre-push:
  commands:
    # Full clippy check
    clippy:
      run: |
        if [[ "$OSTYPE" == "linux-gnu"* ]]; then
          # Running on Linux - execute directly
          CARGO_BUILD_JOBS=2 make clippy
        else
          # Running on macOS - execute in dev container with limited parallelism
          devcontainer exec --workspace-folder . env CARGO_BUILD_JOBS=2 make clippy
        fi
      glob: "*.rs"
      fail_text: "Clippy found issues. Fix them before pushing."
      timeout: 120

    # Library unit tests (full integration tests run in CI)
    test-unit:
      run: |
        if [[ "$OSTYPE" == "linux-gnu"* ]]; then
          # Running on Linux - execute directly
          CARGO_BUILD_JOBS=2 cargo test --lib --all-features --no-fail-fast -- --test-threads=1
        else
          # Running on macOS - execute in dev container with limited parallelism to avoid OOM
          devcontainer exec --workspace-folder . env CARGO_BUILD_JOBS=2 cargo test --lib --all-features --no-fail-fast -- --test-threads=1
        fi
      glob: "*.rs"
      fail_text: "Unit tests failed."
      timeout: 300

    # Build verification
    build-release:
      run: |
        if [[ "$OSTYPE" == "linux-gnu"* ]]; then
          # Running on Linux - execute directly
          CARGO_BUILD_JOBS=2 make build
        else
          # Running on macOS - execute in dev container with limited parallelism
          devcontainer exec --workspace-folder . env CARGO_BUILD_JOBS=2 make build
        fi
      glob: "*.rs"
      fail_text: "Release build failed."
      timeout: 180

  # Execute commands sequentially to avoid OOM in dev container
  parallel: false

# Global settings
skip_output:
  - meta
  - success

# Skip patterns for faster execution
skip_glob:
  - "docs/**/*"
  - "scripts/**/*"
  - "*.md"
  - "*.yml"
  - "*.yaml"
  - "target/**/*"
  - ".github/**/*"
